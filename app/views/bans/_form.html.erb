<%= form_with(model: ban, local: true) do |form| %>
  <% if ban.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ban.errors.count, "error") %> prohibited this ban from being saved:</h2>

      <ul>
      <% ban.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :log %>
    <%= form.text_field :log %>
  </div>

  <div class="field">
    <%= form.label :starttime %>
    <%= form.date_select :starttime %>
  </div>

  <div class="field">
    <%= form.label :endtime %>
    <%= form.date_select :endtime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
