<%= form_with(model: match_participant, local: true) do |form| %>
  <% if match_participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match_participant.errors.count, "error") %> prohibited this match_participant from being saved:</h2>

      <ul>
      <% match_participant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
